# Qwen3-TTS Model Size

Model: `Qwen/Qwen3-TTS-12Hz-1.7B-VoiceDesign`

| Metric | Value |
|---|---|
| Parameters | 1.7B |
| Disk (HF cache) | 4.2 GB |
| RAM (peak RSS) | ~5.2 GB |
| RAM delta (model load) | ~5.1 GB |

Runtime: MLX Audio 0.3.0rc1 on Apple Silicon (macOS).

RAM exceeds disk size due to weight deserialization and Python/MLX overhead.
